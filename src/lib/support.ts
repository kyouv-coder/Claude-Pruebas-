// Canal de soporte para el dueño del negocio (no para sus clientes finales).
// Placeholder por ahora — se termina de configurar por cliente cuando el
// producto se vende a un negocio real, vía estas variables de entorno.
export const SUPPORT_EMAIL = process.env.SUPPORT_EMAIL || "soporte@tu-dominio.com";
export const SUPPORT_WHATSAPP = process.env.SUPPORT_WHATSAPP || "";
